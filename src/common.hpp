#pragma once

#include "options.hpp"

void record(const options &);
void replay(const options &);
void printTrace(const options &);
void pack(const options &);
void unpack(const options &);
void debug(const options &);
